package com.kotlinandroidstarter.app.di.modules

import androidx.fragment.app.FragmentActivity
import org.koin.dsl.module

val UIModule = module {
    
    //factory { (activity: FragmentActivity) -> MainFragmentStateAdapter(activity) }
    
}
